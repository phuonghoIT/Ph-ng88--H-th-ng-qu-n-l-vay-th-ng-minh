$kind: http-request
name: Register
method: POST
url: '{{baseUrl}}/api/auth/register'
order: 1000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "username": "nguyenvana",
      "password": "password123",
      "role": "CUSTOMER",
      "customer": {
        "customerId": 1
      }
    }
  options:
    raw:
      language: json
